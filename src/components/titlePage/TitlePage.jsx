import React from 'react'
import { TitlePageContainer } from './styled'

export default function TitlePage({title}) {
  return (
    <TitlePageContainer>
      {title}
    </TitlePageContainer>
  )
}
